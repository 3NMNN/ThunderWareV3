do local WindUI=loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))();local function Notify(content,duration,title) WindUI:Notify({Title=title or "ThunderWare" ,Content=tostring(content),Duration=duration or 3 ,Icon="bell"});end local Window=WindUI:CreateWindow({Title="ThunderWare v2 [COMPLETE]",Author="Enhanced with All Tabs",Folder="ThunderWare",Icon="zap",Theme="Dark",Size=UDim2.fromOffset(680,500),OpenButton={Title="ThunderWare",Enabled=true,Draggable=true},Topbar={Height=44,ButtonsType="Mac"}});local Tabs={};Tabs.Main=Window:Tab({Title="Main",Icon="home"});Tabs.Player=Window:Tab({Title="Player",Icon="user"});Tabs.Visuals=Window:Tab({Title="Visuals",Icon="eye"});Tabs.Fun=Window:Tab({Title="Fun",Icon="smile"});Tabs.Misc=Window:Tab({Title="Misc",Icon="settings"});Tabs.Utility=Window:Tab({Title="Utility",Icon="wrench"});Tabs.UI=Window:Tab({Title="UI Settings",Icon="palette"});local LeftMain=Tabs.Main:Section({Title="Movement",Box=true,Opened=true});local RightMain=Tabs.Main:Section({Title="Custom Scripts",Box=true,Opened=true});local airStrafeEnabled=false;local airStrafeConnection=nil;local strafeMult=120;local virtualStrafeMult=120;LeftMain:Toggle({Flag="AirStrafe",Title="Air Strafe Acceleration",Desc="Swing your camera in the air to gain speed",Value=false,Callback=function(Value) airStrafeEnabled=Value;if airStrafeEnabled then local Players=game:GetService("Players");local RunService=game:GetService("RunService");local player=Players.LocalPlayer;local camera=workspace.CurrentCamera;local lastCamLook=camera.CFrame.LookVector;airStrafeConnection=RunService.Heartbeat:Connect(function(dt) local character=player.Character;if  not character then return;end local hrp=character:FindFirstChild("HumanoidRootPart");local humanoid=character:FindFirstChildOfClass("Humanoid");if ( not hrp or  not humanoid) then return;end local currentLook=camera.CFrame.LookVector;local flatLook=Vector3.new(currentLook.X,0,currentLook.Z).Unit;local deltaX=currentLook.X-lastCamLook.X ;local deltaZ=currentLook.Z-lastCamLook.Z ;local delta=math.abs(deltaX) + math.abs(deltaZ) ;if ((humanoid.FloorMaterial==Enum.Material.Air) and (delta>0.0005)) then local vel=hrp.AssemblyLinearVelocity;local hspd=Vector3.new(vel.X,0,vel.Z).Magnitude;local boost=flatLook * (hspd + (((strafeMult * delta)/dt) * 0.01)) ;hrp.AssemblyLinearVelocity=Vector3.new(vel.X + (boost.X * delta * 8) ,vel.Y,vel.Z + (boost.Z * delta * 8) );end lastCamLook=currentLook;end);elseif airStrafeConnection then airStrafeConnection:Disconnect();airStrafeConnection=nil;end end});LeftMain:Slider({Flag="StrafeMultiplier",Title="Strafe Multiplier",Value={Min=10,Max=300,Default=120},Step=1,Callback=function(Value) strafeMult=Value;end});local pixelSurfEnabled=false;local pixelSurfConnection=nil;local jumpBlockConn=nil;local SURF_MAX_SPEED=150;local WALL_FRICTION=0.85;local SLIDE_GRAVITY=35;local SLIDE_THRESHOLD=0.85;local WALL_PULL_FORCE=60;LeftMain:Toggle({Flag="PixelSurf",Title="Pixel Surf",Desc="Auto-slide on walls - no input needed",Value=false,Callback=function(Value) pixelSurfEnabled=Value;if pixelSurfEnabled then local Players=game:GetService("Players");local RunService=game:GetService("RunService");local UIS=game:GetService("UserInputService");local player=Players.LocalPlayer;local isSurfing=false;local jumpBlockTimeLeft=0;jumpBlockConn=UIS.InputBegan:Connect(function(input,gp) if ((input.KeyCode==Enum.KeyCode.Space) and isSurfing and  not gp) then if (jumpBlockTimeLeft>0) then local char=player.Character;if char then local hum=char:FindFirstChildOfClass("Humanoid");if hum then hum.Jump=false;end end end end end);pixelSurfConnection=RunService.Heartbeat:Connect(function(dt) local character=player.Character;if  not character then return;end local hrp=character:FindFirstChild("HumanoidRootPart");local humanoid=character:FindFirstChildOfClass("Humanoid");if ( not hrp or  not humanoid) then return;end dt=math.min(dt,0.1);local rayParams=RaycastParams.new();rayParams.FilterDescendantsInstances={character};rayParams.FilterType=Enum.RaycastFilterType.Exclude;local detectionDirs={Vector3.new(1,0,0),Vector3.new( -1,0,0),Vector3.new(0,0,1),Vector3.new(0,0, -1),Vector3.new(1, -0.5,0),Vector3.new( -1, -0.5,0),Vector3.new(0, -0.5,1),Vector3.new(0, -0.5, -1),Vector3.new(1,0.5,0),Vector3.new( -1,0.5,0),Vector3.new(0,0.5,1),Vector3.new(0,0.5, -1),Vector3.new(0.7,0,0.7),Vector3.new( -0.7,0, -0.7),Vector3.new(0.7,0, -0.7),Vector3.new( -0.7,0,0.7)};local surfNormal,closestDist,hitWall=nil,math.huge,false;for _,dir in ipairs(detectionDirs) do local res=workspace:Raycast(hrp.Position,dir:Lerp(Vector3.new(0, -1,0),0.2) * 5 ,rayParams);if res then local dist=(res.Position-hrp.Position).Magnitude;local isWall=math.abs(res.Normal.Y)<SLIDE_THRESHOLD ;if (isWall and (dist<closestDist) and (dist<4.5)) then closestDist=dist;surfNormal=res.Normal;hitWall=true;end end end if (hitWall and surfNormal) then local vel=hrp.AssemblyLinearVelocity;if  not isSurfing then isSurfing=true;jumpBlockTimeLeft=0.2;end humanoid.Jump=false;humanoid.JumpPower=0;local flatVel=Vector3.new(vel.X,0,vel.Z);local speed=flatVel.Magnitude;local projectedVel=flatVel-(surfNormal * flatVel:Dot(surfNormal)) ;local wallSlideDir=projectedVel.Unit;if (projectedVel.Magnitude<0.1) then wallSlideDir=Vector3.new( -surfNormal.Z,0,surfNormal.X).Unit;end local newSpeed=math.min(speed * WALL_FRICTION ,SURF_MAX_SPEED);newSpeed=math.max(newSpeed,20);local slideVel=wallSlideDir * newSpeed ;local yVel=math.max(vel.Y-(SLIDE_GRAVITY * dt) , -50);local wallPushVel= -surfNormal * WALL_PULL_FORCE * dt ;hrp.AssemblyLinearVelocity=slideVel + Vector3.new(wallPushVel.X,yVel,wallPushVel.Z) ;jumpBlockTimeLeft=jumpBlockTimeLeft-dt ;elseif isSurfing then humanoid.JumpPower=50;isSurfing=false;end end);else if pixelSurfConnection then pixelSurfConnection:Disconnect();pixelSurfConnection=nil;end if jumpBlockConn then jumpBlockConn:Disconnect();jumpBlockConn=nil;end local char=game.Players.LocalPlayer.Character;if char then local hum=char:FindFirstChildOfClass("Humanoid");if hum then hum.JumpPower=50;end end end end});loadstring(game:HttpGet("https://raw.githubusercontent.com/LucasXnsLz/XnsLz/refs/heads/main/Pixel%20Surf"))();local virtualStrafeConnection=nil;LeftMain:Toggle({Flag="VirtualStrafe",Title="Virtual Strafe",Desc="Air strafe without moving your camera",Value=false,Callback=function(Value) if Value then local cam=workspace.CurrentCamera;local player=game.Players.LocalPlayer;virtualStrafeConnection=cam:GetPropertyChangedSignal("CFrame"):Connect(function() if (player.Character and player.Character:FindFirstChild("HumanoidRootPart")) then local hrp=player.Character.HumanoidRootPart;local lookVec=cam.CFrame.LookVector;hrp.AssemblyLinearVelocity=Vector3.new(lookVec.X * virtualStrafeMult ,hrp.AssemblyLinearVelocity.Y,lookVec.Z * virtualStrafeMult );end end);elseif virtualStrafeConnection then virtualStrafeConnection:Disconnect();virtualStrafeConnection=nil;end end});LeftMain:Slider({Flag="VirtualStrafeStrength",Title="Virtual Strafe Speed",Value={Min=10,Max=2000,Default=120},Step=1,Callback=function(Value) virtualStrafeMult=Value;end});local easyBounceEnabled=false;local easyBounceConnection=nil;local bouncePower=100;LeftMain:Toggle({Flag="EasyBounce",Title="Easy Bounce",Desc="Auto-bounce when your legs hit the ground",Value=false,Callback=function(Value) easyBounceEnabled=Value;if easyBounceEnabled then local RunService=game:GetService("RunService");local Players=game:GetService("Players");easyBounceConnection=RunService.Heartbeat:Connect(function() local char=Players.LocalPlayer.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");local humanoid=char:FindFirstChildOfClass("Humanoid");if ( not hrp or  not humanoid) then return;end if (humanoid.FloorMaterial~=Enum.Material.Air) then local vel=hrp.AssemblyLinearVelocity;hrp.AssemblyLinearVelocity=Vector3.new(vel.X,bouncePower,vel.Z);end end);Notify("Easy Bounce ON (Power: "   .. bouncePower   .. ")" ,3);else if easyBounceConnection then easyBounceConnection:Disconnect();easyBounceConnection=nil;end Notify("Easy Bounce OFF",3);end end});LeftMain:Slider({Flag="BouncePower",Title="Bounce Power",Value={Min=10,Max=500,Default=100},Step=1,Callback=function(Value) bouncePower=Value;end});local autoTrimpEnabled=false;local autoTrimpConnection=nil;local trimpPower=80;local minTrimpSpeed=0;LeftMain:Toggle({Flag="AutoTrimp",Title="Auto Trimp",Desc="Automatically trimp when falling",Value=false,Callback=function(Value) autoTrimpEnabled=Value;if autoTrimpEnabled then local RunService=game:GetService("RunService");local Players=game:GetService("Players");autoTrimpConnection=RunService.Heartbeat:Connect(function() local char=Players.LocalPlayer.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");local humanoid=char:FindFirstChildOfClass("Humanoid");if ( not hrp or  not humanoid) then return;end local vel=hrp.AssemblyLinearVelocity;local horizontalSpeed=math.sqrt((vel.X^2) + (vel.Z^2) );if ((humanoid.FloorMaterial==Enum.Material.Air) and (horizontalSpeed>=minTrimpSpeed)) then hrp.AssemblyLinearVelocity=Vector3.new(vel.X,trimpPower,vel.Z);end end);elseif autoTrimpConnection then autoTrimpConnection:Disconnect();autoTrimpConnection=nil;end end});LeftMain:Slider({Flag="TrimpPower",Title="Trimp Power",Value={Min=10,Max=200,Default=80},Step=1,Callback=function(v) trimpPower=v;end});LeftMain:Slider({Flag="MinTrimpSpeed",Title="Min Speed",Value={Min=0,Max=100,Default=0},Step=1,Callback=function(v) minTrimpSpeed=v;end});local disabledWalls={};local LeftMain2=Tabs.Main:Section({Title="Other",Box=true,Opened=true});LeftMain2:Button({Title="Remove Invisible Walls",Callback=function() disabledWalls={};local removed=0;local player=game.Players.LocalPlayer;local char=player.Character;local function isInvisibleWall(part) if  not part:IsA("BasePart") then return false;end if (char and part:IsDescendantOf(char)) then return false;end if (part.Transparency~=1) then return false;end if  not part.CanCollide then return false;end for _,child in ipairs(part:GetChildren()) do if (child:IsA("Script") or child:IsA("LocalScript") or child:IsA("ModuleScript") or child:IsA("SpecialMesh") or child:IsA("UnionOperation")) then return false;end end return true;end for _,obj in ipairs(workspace:GetDescendants()) do if isInvisibleWall(obj) then obj.CanCollide=false;table.insert(disabledWalls,obj);removed=removed + 1 ;end end Notify("Removed collision from "   .. removed   .. " wall(s)." ,4);end});LeftMain2:Button({Title="Restore Walls",Callback=function() local restored=0;for _,part in ipairs(disabledWalls) do if (part and part.Parent) then part.CanCollide=true;restored=restored + 1 ;end end disabledWalls={};Notify("Restored "   .. restored   .. " wall(s)." ,4);end});LeftMain2:Space();local LegacySection=Tabs.Main:Section({Title="Legacy",Box=true,Opened=true});local evadeSpeedValue=1500;LegacySection:Slider({Flag="EvadeSpeedValue",Title="Evade Speed Override",Value={Min=100,Max=5000,Default=1500},Step=1,Callback=function(Value) evadeSpeedValue=Value;end});LegacySection:Button({Title="Apply Evade Speed Changer",Callback=function() _G.RealSpeedOverride=evadeSpeedValue;game.Players.LocalPlayer.CharacterAdded:Connect(function(char) char:WaitForChild("Humanoid");char.Humanoid:GetAttributeChangedSignal("RealSpeed"):Connect(function() if (char.Humanoid:GetAttribute("RealSpeed")~=_G.RealSpeedOverride) then char.Humanoid:SetAttribute("RealSpeed",_G.RealSpeedOverride);end end);char.Humanoid:SetAttribute("RealSpeed",_G.RealSpeedOverride);if (char:FindFirstChild("Communicator") and char.Communicator:IsA("RemoteEvent")) then local oldFire=char.Communicator.FireServer;char.Communicator.FireServer=function(self,...) local args={...};if (args[1]=="update") then task.wait();if char.Humanoid then char.Humanoid:SetAttribute("RealSpeed",_G.RealSpeedOverride);char.Humanoid:SetAttribute("RealJumpHeight",char.Humanoid:GetAttribute("RealJumpHeight") or 0 );end return _G.RealSpeedOverride,char.Humanoid:GetAttribute("RealJumpHeight");end return oldFire(self,unpack(args));end;end end);task.spawn(function() while true do task.wait(0.5);local plr=game.Players.LocalPlayer;if (plr and plr.Character and plr.Character:FindFirstChild("Humanoid")) then local hum=plr.Character.Humanoid;if (hum:GetAttribute("RealSpeed")~=_G.RealSpeedOverride) then hum:SetAttribute("RealSpeed",_G.RealSpeedOverride);end end end end);Notify("Evade Speed Changer applied! Speed: "   .. evadeSpeedValue ,3);end});local edgeTrimpEnabled=false;local edgeTrimpConnection=nil;local edgeTrimpInput=nil;local BOOST_POWER=100;local MIN_Y_VELOCITY_TO_ACTIVATE=1;RightMain:Toggle({Flag="EdgeTrimp",Title="Edge Trimp",Desc="Boost upward when falling on edges (Press U to toggle)",Value=false,Callback=function(Value) edgeTrimpEnabled=Value;if edgeTrimpEnabled then local Players=game:GetService("Players");local RunService=game:GetService("RunService");local UserInputService=game:GetService("UserInputService");local player=Players.LocalPlayer;local function getCharacterAndHrp() local character=player.Character or player.CharacterAdded:Wait() ;return character,character:WaitForChild("HumanoidRootPart"),character:WaitForChild("Humanoid");end local character,hrp,humanoid=getCharacterAndHrp();local UP_VELOCITY=Vector3.new(0,BOOST_POWER,0);local function isBouncePart(part) local bounceKeywords={"bounce","boost","launch","jump","pad","ramp","platform"};local name=string.lower(part.Name);for _,keyword in pairs(bounceKeywords) do if name:find(keyword) then return true;end end if (part.Parent and (part.Parent:IsA("Model") or part.Parent:IsA("Folder"))) then local parentName=string.lower(part.Parent.Name);for _,keyword in pairs(bounceKeywords) do if parentName:find(keyword) then return true;end end end return false;end local function applyEdgeBoost() if ( not edgeTrimpEnabled or  not hrp or  not humanoid or (humanoid.Health<=0)) then return;end if (hrp.AssemblyLinearVelocity.Y< -MIN_Y_VELOCITY_TO_ACTIVATE) then local touchingParts=hrp:GetTouchingParts();local isTouchingEdge,isTouchingBounce=false,false;if ( #touchingParts>0) then for _,part in pairs(touchingParts) do if isBouncePart(part) then isTouchingBounce=true;break;end end if  not isTouchingBounce then isTouchingEdge=true;end end if isTouchingEdge then local h=Vector3.new(hrp.AssemblyLinearVelocity.X,0,hrp.AssemblyLinearVelocity.Z);hrp.AssemblyLinearVelocity=h + UP_VELOCITY ;end end end edgeTrimpConnection=RunService.Stepped:Connect(applyEdgeBoost);edgeTrimpInput=UserInputService.InputBegan:Connect(function(input,gameProcessed) if (gameProcessed or (input.KeyCode~=Enum.KeyCode.U)) then return;end edgeTrimpEnabled= not edgeTrimpEnabled;Notify((edgeTrimpEnabled and ("Edge Trimp ON (Power: "   .. BOOST_POWER   .. ")")) or "Edge Trimp OFF" ,3);end);player.CharacterAdded:Connect(function(newCharacter) if edgeTrimpEnabled then character=newCharacter;hrp=character:WaitForChild("HumanoidRootPart");humanoid=character:WaitForChild("Humanoid");end end);Notify("Edge Trimp ON — Press U to toggle (Power: "   .. BOOST_POWER   .. ")" ,3);else if edgeTrimpConnection then edgeTrimpConnection:Disconnect();edgeTrimpConnection=nil;end if edgeTrimpInput then edgeTrimpInput:Disconnect();edgeTrimpInput=nil;end Notify("Edge Trimp OFF",3);end end});RightMain:Slider({Flag="EdgeTrimpPower",Title="Trimp Boost Power",Value={Min=10,Max=300,Default=100},Step=1,Callback=function(Value) BOOST_POWER=Value;end});RightMain:Button({Title="Downed Dash (Hold Ctrl)",Callback=function() local ok,err=pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/E5alR9/E5s/refs/heads/main/donwedslide"))();end);if ok then Notify("✅ Downed Dash loaded! Hold CTRL to dash",3);else Notify("❌ Failed to load Downed Dash: "   .. tostring(err) ,4);end end});RightMain:Button({Title="Cactus Hitbox Expander",Callback=function() loadstring(game:HttpGet("https://raw.githubusercontent.com/E5alR9/E5s/refs/heads/main/cacbhop"))();end});local LeftPlayer=Tabs.Player:Section({Title="Player",Box=true,Opened=true});local RightPlayer=Tabs.Player:Section({Title="Modification",Box=true,Opened=true});local cameraShakeDisabled=false;local cameraShakeConn=nil;LeftPlayer:Toggle({Flag="DisableCameraShake",Title="Disable Camera Shake",Desc="Remove camera shake effects",Value=false,Callback=function(Value) cameraShakeDisabled=Value;if cameraShakeDisabled then local RunService=game:GetService("RunService");local cam=workspace.CurrentCamera;cameraShakeConn=RunService.RenderStepped:Connect(function() if cameraShakeDisabled then cam.CFrame=cam.CFrame;end end);Notify("Camera Shake Disabled",3);else if cameraShakeConn then cameraShakeConn:Disconnect();cameraShakeConn=nil;end Notify("Camera Shake Enabled",3);end end});LeftPlayer:Toggle({Flag="FullBright",Title="Full Bright",Desc="Brighten the entire game",Value=false,Callback=function(Value) pcall(function() local Lighting=game:GetService("Lighting");if Value then Lighting.Brightness=2;Lighting.ClockTime=12;Lighting.Ambient=Color3.fromRGB(255,255,255);Lighting.OutdoorAmbient=Color3.fromRGB(255,255,255);Notify("Full Bright Enabled",3);else Lighting.Brightness=1;Lighting.ClockTime=14;Lighting.Ambient=Color3.fromRGB(200,200,200);Lighting.OutdoorAmbient=Color3.fromRGB(200,200,200);Notify("Full Bright Disabled",3);end end);end});local speedValue=0;local jumpHeightValue=3;local jumpCapValue=6;local jumpSpeedMultiplier=1.45;local strafeAccelerationValue=182;local airAccelerationValue=1;local sprintCapValue=2;local sprintAccelerationValue=1;RightPlayer:Slider({Flag="Speed",Title="Speed",Value={Min=0,Max=1000,Default=0},Step=1,Callback=function(v) speedValue=v;end});RightPlayer:Slider({Flag="JumpHeight",Title="Jump height",Value={Min=0,Max=100,Default=3},Step=0.1,Callback=function(v) jumpHeightValue=v;end});RightPlayer:Slider({Flag="JumpCap",Title="Jump Cap",Value={Min=0,Max=50,Default=6},Step=1,Callback=function(v) jumpCapValue=v;end});RightPlayer:Slider({Flag="JumpSpeedMultiplier",Title="Jump Speed Multiplier",Value={Min=0.1,Max=5,Default=1.45},Step=0.01,Callback=function(v) jumpSpeedMultiplier=v;end});RightPlayer:Slider({Flag="StrafeAcceleration",Title="Strafe Acceleration",Value={Min=0,Max=500,Default=182},Step=1,Callback=function(v) strafeAccelerationValue=v;end});RightPlayer:Slider({Flag="AirAcceleration",Title="Air Acceleration",Value={Min=0,Max=10,Default=1},Step=0.01,Callback=function(v) airAccelerationValue=v;end});RightPlayer:Slider({Flag="SprintCap",Title="Sprint Cap",Value={Min=0,Max=50,Default=2},Step=1,Callback=function(v) sprintCapValue=v;end});RightPlayer:Slider({Flag="SprintAcceleration",Title="Sprint Acceleration",Value={Min=0,Max=10,Default=1},Step=0.01,Callback=function(v) sprintAccelerationValue=v;end});local LeftVis=Tabs.Visuals:Section({Title="Emote Swapper",Box=true,Opened=true});local RightVis=Tabs.Visuals:Section({Title="Skybox & Shader",Box=true,Opened=true});local ownedEmote,wantedEmote="","";LeftVis:Input({Flag="OwnedEmote",Title="Owned Emote",Placeholder="Emote you own",Value="",Callback=function(v) ownedEmote=v;end});LeftVis:Input({Flag="WantedEmote",Title="Wanted Emote",Placeholder="Emote you want",Value="",Callback=function(v) wantedEmote=v;pcall(function() game:GetService("ReplicatedStorage").Remotes.PlayEmote:FireServer(v);end);end});LeftVis:Button({Title="Apply Emote Swap",Callback=function() if ((ownedEmote=="") or (wantedEmote=="")) then Notify("Fill in both emote fields first!",3);return;end local ok,err=pcall(function() local emotes=game:GetService("ReplicatedStorage").Items.Emotes;local ownedObj=emotes:FindFirstChild(ownedEmote);if ownedObj then ownedObj.Name=ownedEmote   .. "_Backup" ;end emotes[wantedEmote].Name=ownedEmote;end);Notify((ok and "Emote swapped successfully!") or ("Failed: "   .. tostring(err)) ,(ok and 3) or 4 );end});local cosmetic1,cosmetic2="","";local originalCosmetic1,originalCosmetic2="","";local isSwapped=false;local function normalize(str) return str:gsub("%s+",""):lower();end local function levenshtein(s,t) local m,n= #s, #t;local d={};for i=0,m do d[i]={[0]=i};end for j=0,n do d[0][j]=j;end for i=1,m do for j=1,n do local cost=((s:sub(i,i)==t:sub(j,j)) and 0) or 1 ;d[i][j]=math.min(d[i-1 ][j] + 1 ,d[i][j-1 ] + 1 ,d[i-1 ][j-1 ] + cost );end end return d[m][n];end local function similarity(s,t) local nS,nT=normalize(s),normalize(t);return 1 -(levenshtein(nS,nT)/math.max( #nS, #nT)) ;end local function findSimilar(name) local Cosmetics=game:GetService("ReplicatedStorage"):WaitForChild("Items"):WaitForChild("Cosmetics");local bestMatch,bestScore=name,0.5;for _,c in ipairs(Cosmetics:GetChildren()) do local score=similarity(name,c.Name);if (score>bestScore) then bestScore=score;bestMatch=c.Name;end end return bestMatch;end local CosmeticSection=Tabs.Visuals:Section({Title="Cosmetic Changer",Box=true,Opened=true});CosmeticSection:Input({Flag="CosmeticOwned",Title="Cosmetic You Own",Placeholder="e.g., ToxicInferno",Value="",Callback=function(v) cosmetic1=v;if  not isSwapped then originalCosmetic1=v;end end});CosmeticSection:Input({Flag="CosmeticWanted",Title="Cosmetic You Want",Placeholder="e.g., Broom",Value="",Callback=function(v) cosmetic2=v;if  not isSwapped then originalCosmetic2=v;end end});CosmeticSection:Button({Title="Apply Cosmetic Swap",Callback=function() pcall(function() if ((cosmetic1=="") or (cosmetic2=="") or (cosmetic1==cosmetic2)) then Notify("Enter two different cosmetics!",3);return;end local Cosmetics=game:GetService("ReplicatedStorage"):WaitForChild("Items"):WaitForChild("Cosmetics");cosmetic1=findSimilar(cosmetic1);cosmetic2=findSimilar(cosmetic2);local a,b=Cosmetics:FindFirstChild(cosmetic1),Cosmetics:FindFirstChild(cosmetic2);if ( not a or  not b) then Notify("Cosmetics not found!",3);return;end if  not isSwapped then originalCosmetic1=cosmetic1;originalCosmetic2=cosmetic2;end local tempRoot=Instance.new("Folder",Cosmetics);tempRoot.Name="__temp_swap_"   .. tostring(tick()):gsub("%.","_") ;local tempA,tempB=Instance.new("Folder",tempRoot),Instance.new("Folder",tempRoot);for _,c in ipairs(a:GetChildren()) do c.Parent=tempA;end for _,c in ipairs(b:GetChildren()) do c.Parent=tempB;end for _,c in ipairs(tempA:GetChildren()) do c.Parent=b;end for _,c in ipairs(tempB:GetChildren()) do c.Parent=a;end tempRoot:Destroy();isSwapped=true;Notify("Swapped "   .. cosmetic1   .. " with "   .. cosmetic2 ,3);end);end});local LeftFun=Tabs.Fun:Section({Title="Legacy Features",Box=true,Opened=true});local RightFun=Tabs.Fun:Section({Title="Legacy Tweaks",Box=true,Opened=true});local oldSpeedSystemEnabled=false;local oldSpeedConnection=nil;local oldSpeed=20;LeftFun:Toggle({Flag="OldSpeedSystem",Title="Old Speed System",Desc="Enable legacy speed system from old game version",Value=false,Callback=function(Value) oldSpeedSystemEnabled=Value;if oldSpeedSystemEnabled then local Players=game:GetService("Players");local RunService=game:GetService("RunService");oldSpeedConnection=RunService.Heartbeat:Connect(function() local player=Players.LocalPlayer;local char=player.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");local humanoid=char:FindFirstChildOfClass("Humanoid");if ( not hrp or  not humanoid) then return;end local moveDir=humanoid.MoveDirection;if (moveDir.Magnitude>0) then local vel=hrp.AssemblyLinearVelocity;local newVel=moveDir * oldSpeed ;hrp.AssemblyLinearVelocity=Vector3.new(newVel.X,vel.Y,newVel.Z);end end);Notify("✅ Old Speed System enabled!",3);else if oldSpeedConnection then oldSpeedConnection:Disconnect();oldSpeedConnection=nil;end Notify("❌ Old Speed System disabled!",3);end end});LeftFun:Slider({Flag="OldSpeed",Title="Legacy Speed Value",Value={Min=5,Max=100,Default=20},Step=1,Callback=function(Value) oldSpeed=Value;end});local oldJumpSystemEnabled=false;local oldJumpConnection=nil;local oldJumpHeight=50;LeftFun:Toggle({Flag="OldJumpSystem",Title="Old Jump System",Desc="Use legacy jump mechanics",Value=false,Callback=function(Value) oldJumpSystemEnabled=Value;if oldJumpSystemEnabled then local Players=game:GetService("Players");local UIS=game:GetService("UserInputService");local player=Players.LocalPlayer;oldJumpConnection=UIS.InputBegan:Connect(function(input,gameProcessed) if gameProcessed then return;end if (input.KeyCode==Enum.KeyCode.Space) then local char=player.Character;if char then local hrp=char:FindFirstChild("HumanoidRootPart");local humanoid=char:FindFirstChildOfClass("Humanoid");if (hrp and humanoid and (humanoid.FloorMaterial~=Enum.Material.Air)) then local vel=hrp.AssemblyLinearVelocity;hrp.AssemblyLinearVelocity=Vector3.new(vel.X,oldJumpHeight,vel.Z);end end end end);Notify("✅ Old Jump System enabled!",3);else if oldJumpConnection then oldJumpConnection:Disconnect();oldJumpConnection=nil;end Notify("❌ Old Jump System disabled!",3);end end});LeftFun:Slider({Flag="OldJumpHeight",Title="Legacy Jump Height",Value={Min=10,Max=150,Default=50},Step=1,Callback=function(Value) oldJumpHeight=Value;end});local oldAnimSpeedEnabled=false;local oldAnimConnections={};local oldAnimSpeed=1.5;LeftFun:Toggle({Flag="OldAnimSpeed",Title="Old Animation Speed",Desc="Restore legacy animation speed",Value=false,Callback=function(Value) oldAnimSpeedEnabled=Value;if oldAnimSpeedEnabled then local Players=game:GetService("Players");local player=Players.LocalPlayer;local function applyOldAnimSpeed(char) if ( not char or  not oldAnimSpeedEnabled) then return;end local humanoid=char:FindFirstChildOfClass("Humanoid");if humanoid then for _,anim in ipairs(humanoid:GetPlayingAnimationTracks()) do anim.Speed=oldAnimSpeed;end end end if player.Character then applyOldAnimSpeed(player.Character);end local charConn=player.CharacterAdded:Connect(function(char) if oldAnimSpeedEnabled then task.wait(0.1);applyOldAnimSpeed(char);end end);table.insert(oldAnimConnections,charConn);Notify("✅ Old Animation Speed enabled!",3);else for _,conn in ipairs(oldAnimConnections) do if conn.Connected then conn:Disconnect();end end table.clear(oldAnimConnections);Notify("❌ Old Animation Speed disabled!",3);end end});LeftFun:Slider({Flag="OldAnimSpeedValue",Title="Animation Speed Multiplier",Value={Min=0.5,Max=3,Default=1.5},Step=0.1,Callback=function(Value) oldAnimSpeed=Value;end});local oldPhysicsEnabled=false;local originalDamping={};RightFun:Toggle({Flag="OldPhysics",Title="Restore Old Physics",Desc="Revert to legacy physics engine behavior",Value=false,Callback=function(Value) oldPhysicsEnabled=Value;if oldPhysicsEnabled then pcall(function() local player=game.Players.LocalPlayer;local char=player.Character;if char then for _,part in ipairs(char:FindFirstChild("HumanoidRootPart"):GetConnectedParts()) do originalDamping[part]={CustomPhysicalProperties=part.CustomPhysicalProperties};end end end);Notify("✅ Old Physics enabled!",3);else originalDamping={};Notify("❌ Old Physics disabled!",3);end end});local removeNewUIEnabled=false;RightFun:Toggle({Flag="RemoveNewUI",Title="Remove New UI Elements",Desc="Hide new UI features",Value=false,Callback=function(Value) removeNewUIEnabled=Value;if removeNewUIEnabled then pcall(function() local gui=game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui");local removed=0;for _,obj in ipairs(gui:GetDescendants()) do if (obj:IsA("ScreenGui") or obj:IsA("Frame")) then local name=obj.Name:lower();if (name:find("new") or name:find("update") or name:find("overhaul")) then obj.Visible=false;removed=removed + 1 ;end end end Notify("Hid "   .. removed   .. " UI element(s)" ,3);end);else pcall(function() local gui=game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui");for _,obj in ipairs(gui:GetDescendants()) do obj.Visible=true;end end);Notify("Restored UI elements",3);end end});local legacyWalkSpeedEnabled=false;local legacyWalkConnection=nil;local legacyWalkSpeed=16;RightFun:Toggle({Flag="LegacyWalkSpeed",Title="Legacy Walk Speed",Desc="Use old walking speed",Value=false,Callback=function(Value) legacyWalkSpeedEnabled=Value;if legacyWalkSpeedEnabled then local Players=game:GetService("Players");local RunService=game:GetService("RunService");legacyWalkConnection=RunService.Heartbeat:Connect(function() local player=Players.LocalPlayer;local char=player.Character;if  not char then return;end local humanoid=char:FindFirstChildOfClass("Humanoid");if humanoid then humanoid.WalkSpeed=legacyWalkSpeed;end end);Notify("✅ Legacy Walk Speed enabled! ("   .. legacyWalkSpeed   .. ")" ,3);else if legacyWalkConnection then legacyWalkConnection:Disconnect();legacyWalkConnection=nil;end pcall(function() local char=game.Players.LocalPlayer.Character;if char then local humanoid=char:FindFirstChildOfClass("Humanoid");if humanoid then humanoid.WalkSpeed=16;end end end);Notify("❌ Legacy Walk Speed disabled!",3);end end});RightFun:Slider({Flag="LegacyWalkSpeedValue",Title="Walk Speed Value",Value={Min=5,Max=50,Default=16},Step=1,Callback=function(Value) legacyWalkSpeed=Value;end});local LeftMisc=Tabs.Misc:Section({Title="Config Management",Box=true,Opened=true});local RightMisc=Tabs.Misc:Section({Title="Quick Utilities",Box=true,Opened=true});local configs={};local currentConfigName="";LeftMisc:Input({Flag="ConfigName",Title="Config Name",Placeholder="Enter config name",Value="",Callback=function(Value) currentConfigName=Value;end});LeftMisc:Button({Title="Create Config",Callback=function() if (currentConfigName=="") then Notify("Enter a config name first!",3);return;end if configs[currentConfigName] then Notify("Config already exists!",3);return;end local configData={name=currentConfigName,airStrafeEnabled=airStrafeEnabled,strafeMult=strafeMult,pixelSurfEnabled=pixelSurfEnabled,timestamp=os.date("%Y-%m-%d %H:%M:%S"),created=os.time()};configs[currentConfigName]=configData;Notify('✅ Config \"'   .. currentConfigName   .. '\" created!' ,3);end});LeftMisc:Button({Title="Save Config",Callback=function() if (currentConfigName=="") then Notify("Enter a config name first!",3);return;end if  not configs[currentConfigName] then Notify("Config not found! Create it first.",3);return;end configs[currentConfigName]={name=currentConfigName,airStrafeEnabled=airStrafeEnabled,strafeMult=strafeMult,pixelSurfEnabled=pixelSurfEnabled,timestamp=os.date("%Y-%m-%d %H:%M:%S"),created=configs[currentConfigName].created};Notify('💾 Config \"'   .. currentConfigName   .. '\" saved!' ,3);end});LeftMisc:Button({Title="Load Config",Callback=function() if (currentConfigName=="") then Notify("Enter a config name first!",3);return;end if  not configs[currentConfigName] then Notify("Config not found!",3);return;end local config=configs[currentConfigName];strafeMult=config.strafeMult or 120 ;Notify('📂 Config \"'   .. currentConfigName   .. '\" loaded!' ,3);end});RightMisc:Button({Title="Game Info",Callback=function() local game_id=game.GameId;local place_id=game.PlaceId;Notify("Game ID: "   .. game_id   .. " | Place ID: "   .. place_id ,4);end});RightMisc:Button({Title="Player Count",Callback=function() local players=game:GetService("Players"):GetPlayers();Notify("Players in game: "   ..  #players ,3);end});RightMisc:Button({Title="Reset Character",Callback=function() local player=game.Players.LocalPlayer;local char=player.Character;if char then local hum=char:FindFirstChildOfClass("Humanoid");if hum then hum.Health=0;end end Notify("Character reset!",3);end});RightMisc:Button({Title="Rejoin Game",Callback=function() local TeleportService=game:GetService("TeleportService");local PlaceId=game.PlaceId;TeleportService:Teleport(PlaceId);end});local LeftUtil=Tabs.Utility:Section({Title="Item Utilities",Box=true,Opened=true});local RightUtil=Tabs.Utility:Section({Title="Teleports",Box=true,Opened=true});LeftUtil:Button({Title="Unlimited Cola",Callback=function() local ok,err=pcall(function() local player=game.Players.LocalPlayer;local char=player.Character;if  not char then return;end local humanoid=char:FindFirstChildOfClass("Humanoid");if humanoid then humanoid:SetAttribute("UnlimitedCola",true);Notify("✅ Unlimited Cola activated!",3);end end);if  not ok then Notify("Error: "   .. tostring(err) ,4);end end});LeftUtil:Button({Title="Cola Speed Booster",Callback=function() local ok,err=pcall(function() local player=game.Players.LocalPlayer;local char=player.Character;if  not char then return;end local humanoid=char:FindFirstChildOfClass("Humanoid");local hrp=char:FindFirstChild("HumanoidRootPart");if (humanoid and hrp) then humanoid:SetAttribute("ColaSpeedBoost",true);local vel=hrp.AssemblyLinearVelocity;hrp.AssemblyLinearVelocity=Vector3.new(vel.X * 1.5 ,vel.Y,vel.Z * 1.5 );Notify("🚀 Cola Speed Booster x1.5!",3);end end);if  not ok then Notify("Error: "   .. tostring(err) ,4);end end});RightUtil:Button({Title="Teleport to Spawn",Callback=function() local ok,err=pcall(function() local player=game.Players.LocalPlayer;local char=player.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");if  not hrp then return;end local spawn=workspace:FindFirstChildOfClass("SpawnLocation") or workspace:FindFirstChild("Spawn") ;if spawn then hrp.CFrame=spawn.CFrame + Vector3.new(0,5,0) ;else hrp.CFrame=CFrame.new(0,10,0);end end);Notify((ok and "Teleported to Spawn!") or ("Error: "   .. tostring(err)) ,3);end});RightUtil:Button({Title="Teleport to Random Player",Callback=function() local players=game:GetService("Players"):GetPlayers();local localPlayer=game.Players.LocalPlayer;local others={};for _,p in ipairs(players) do if ((p~=localPlayer) and p.Character and p.Character:FindFirstChild("HumanoidRootPart")) then table.insert(others,p);end end if ( #others==0) then Notify("No other players found!",3);return;end local target=others[math.random(1, #others)];local hrp=localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") ;if hrp then hrp.CFrame=target.Character.HumanoidRootPart.CFrame + Vector3.new(0,3,0) ;Notify("Teleported to "   .. target.Name   .. "!" ,3);end end});RightUtil:Button({Title="Teleport to Downed Player",Callback=function() local players=game:GetService("Players"):GetPlayers();local localPlayer=game.Players.LocalPlayer;local target=nil;for _,p in ipairs(players) do if ((p~=localPlayer) and p.Character) then local hum=p.Character:FindFirstChildOfClass("Humanoid");if (hum and ((hum.Health<=0) or (hum:GetState()==Enum.HumanoidStateType.Dead))) then target=p;break;end end end if  not target then Notify("No downed players found!",3);return;end local hrp=localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart") ;if (hrp and target.Character:FindFirstChild("HumanoidRootPart")) then hrp.CFrame=target.Character.HumanoidRootPart.CFrame + Vector3.new(0,3,0) ;Notify("Teleported to downed: "   .. target.Name   .. "!" ,3);end end});RightUtil:Button({Title="Teleport to Ticket",Callback=function() local target=workspace:FindFirstChild("Ticket",true) or workspace:FindFirstChild("ticket",true) ;if  not target then Notify("No Ticket found!",3);return;end local hrp=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if hrp then local pos=(target:IsA("BasePart") and target.Position) or (target:IsA("Model") and target:GetModelCFrame().Position) ;if pos then hrp.CFrame=CFrame.new(pos + Vector3.new(0,5,0) );end Notify("Teleported to Ticket!",3);end end});RightUtil:Button({Title="Teleport to Nextbot",Callback=function() local target=nil;for _,kw in ipairs({"nextbot","Nextbot","NextBot","npc","NPC"}) do target=workspace:FindFirstChild(kw,true);if target then break;end end if  not target then Notify("No Nextbot found!",3);return;end local hrp=game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") ;if hrp then local pos=(target:IsA("BasePart") and target.Position) or (target:IsA("Model") and target:GetModelCFrame().Position) ;if pos then hrp.CFrame=CFrame.new(pos + Vector3.new(0,5,0) );end Notify("Teleported to Nextbot!",3);end end});local UISettings=Tabs.UI:Section({Title="UI Configuration",Box=true,Opened=true});local ConfigManager=Window.ConfigManager;local ConfigName="default";local ConfigNameInput=UISettings:Input({Flag="ThunderWareConfigName",Title="Config Name",Placeholder="default",Value=ConfigName,Callback=function(value) ConfigName=((value~="") and value) or "default" ;end});UISettings:Space();local AllConfigsDropdown=UISettings:Dropdown({Flag="ThunderWareAllConfigs",Title="Saved Configs",Values=ConfigManager:AllConfigs(),AllowNone=true,Callback=function(value) if value then ConfigName=value;ConfigNameInput:Set(value);end end});UISettings:Button({Title="Save Config",Icon="save",Callback=function() Window.CurrentConfig=ConfigManager:Config(ConfigName);if Window.CurrentConfig:Save() then Notify("Config '"   .. ConfigName   .. "' saved!" ,3,"Saved");AllConfigsDropdown:Refresh(ConfigManager:AllConfigs());end end});UISettings:Button({Title="Load Config",Icon="refresh-cw",Callback=function() Window.CurrentConfig=ConfigManager:CreateConfig(ConfigName);if Window.CurrentConfig:Load() then Notify("Config '"   .. ConfigName   .. "' loaded!" ,3,"Loaded");end end});UISettings:Dropdown({Flag="ThunderWareTheme",Title="Theme",Values={"Dark","Light"},Value="Dark",Callback=function(value) WindUI:SetTheme(value);end});Notify("🎮 ThunderWare v2 [COMPLETE] loaded with ALL features!",5); end
